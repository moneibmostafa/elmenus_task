const UserAdapter = require('./user.adapter');
const ItemAdapter = require('./item.adapter');
const OrderAdapter = require('./order.adapter');

module.exports = {
  UserAdapter,
  ItemAdapter,
  OrderAdapter,
};
