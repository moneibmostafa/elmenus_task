const UserAdapter = require('./user.adapter');
const ItemAdapter = require('./item.adapter');
const OrderAdapter = require('./checkout.adapter');

module.exports = {
  UserAdapter,
  ItemAdapter,
  OrderAdapter,
};
